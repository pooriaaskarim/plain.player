part of 'state.plain.dart';

class AppLaunchState extends PlainState {
  /// Plain's Launch State.
  const AppLaunchState._({
    required final Settings defaultSettings,
  }) : super(
          settings: defaultSettings,
        );
}
